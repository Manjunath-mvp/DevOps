pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Manjunath-mvp/cyber_security.git'
            }
        }

        stage('Status') {
            steps {
                echo "Successfully fetched the repo"
            }
        }
    }
}
