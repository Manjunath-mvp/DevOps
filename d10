mkdir DevOpsFramework
cd DevOpsFramework
git init
git add .
git commit -m "Initial Commit"
git remote add origin https://github.com/student/DevOpsFramework.git
git push -u origin main

mvn clean compile
mvn test
mvn package

docker build -t devops-app .
docker run -d -p 8080:8080 devops-app
docker ps
docker stop <container_id>
docker rm <container_id>
